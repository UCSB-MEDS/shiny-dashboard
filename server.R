# server instructions
server <- function(input, output, session){
  
  # observeEvent(input$career_db, {
  #   if(input$career_db == "CAREER"){
  #     updateTabItems(session, "tabs", selected = "hidden_career")
  #   }
  # 
  # }) # EO OE
  
} # EO server